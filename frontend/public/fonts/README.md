# Proxima Nova webfonts

Place your licensed `.woff2` files here with these exact names:

- `ProximaNova-Regular.woff2`
- `ProximaNova-Semibold.woff2`
- `ProximaNova-Bold.woff2`

Then restart the dev server.

The app is already configured to use these files via `@font-face` in `app/globals.css`.
